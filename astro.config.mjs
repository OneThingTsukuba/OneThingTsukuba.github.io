import { defineConfig } from 'astro/config';

export default defineConfig({
  // This is an organization Pages repository, so GitHub Pages serves it from the domain root.
  site: 'https://onethingtsukuba.github.io',
});
